# RELYR IPC設計書（実装済みの継続契約）

## 1. 状況と目的

管理者版RELYRでは、`ChangeWindowMessageFilterEx` が3種類とも成功しているにもかかわらず、Explorerから `WM_DROPFILES` が届かなかった。通常権限版では同じドロップが動作している。

目的は、Deckの設定画面・Deckオーバーレイを非管理者プロセスに置き、Explorerとのドラッグ＆ドロップを受けられるようにすること。そのうえで、既存のグローバルフックや管理者ウィンドウへの限定アクションだけを管理者ヘルパーへ依頼する。

プロセス分離と名前付きパイプは実装済みである。本書は、今後の変更で権限境界、接続元検証、許可コマンド、ライフサイクルを崩さないための継続契約として扱う。

## 2. プロセス構成

### UIプロセス（非管理者）

- 通常権限、現在のユーザーセッションで起動する。
- メイン画面、Deck設定画面、Deckオーバーレイ、Explorerからのファイルドロップを担当する。
- ファイルパスを受け取り、既存のDeck設定保存経路を使って保存する。
- 管理者権限が不要な表示・編集処理はすべてUIプロセスで行う。

### アクションヘルパー（管理者）

- UIプロセスが明示的に起動した同一ユーザーの管理者プロセス。
- グローバル入力フック、限定されたキー注入、限定された対象ウィンドウ操作だけを担当する。
- Explorerからのファイルを直接受け取らない。
- UIプロセスから切断された場合は孤児化せず終了する。

既存の起動タスク方式を再利用できるかを先に確認し、別の常駐サービスは新設しない。

## 3. IPCエンドポイント

- Named pipeを使用する。
- 固定名は使用せず、UIプロセスが起動ごとに暗号学的にランダムなIDを生成する。
- 形式は `\\.\pipe\RELYR-{SessionId}-{RandomId}` とする。
- パイプ名や認証秘密を一般的な固定ファイル、URL、コマンド内容には保存しない。
- 通信は単一のUIプロセスと単一のヘルパーだけに限定し、同時接続は1本とする。

## 4. パイプのアクセス制御

`NamedPipeServerStream` 作成時に明示的な `PipeSecurity` を設定する。

- 現在のWindowsユーザーSIDに対してのみ接続・読み書きを許可する。
- `Everyone`、`Authenticated Users`、全ローカルユーザーへの許可は付与しない。
- 必要な場合のみ、現在のユーザーのSIDと同一セッションを条件にする。
- パイプ作成後にACLを読み戻し、想定外のACEがあれば通信を開始せず終了する。
- パイプは暗号化された通信ではないため、認証秘密とコマンド検証を必須にする。

同一ユーザーの別プロセスまではACLだけで区別できないため、次の接続元検証も行う。

## 5. 接続元プロセス検証とハンドシェイク

1. UIプロセスが256ビットのランダムなセッション秘密を生成する。
2. 秘密はコマンドラインや通常の設定ファイルには渡さず、起動時の継承可能な一時通信経路でヘルパーへ渡す。
3. ヘルパーがパイプへ接続したら、プロトコルバージョン、UIプロセスID、セッションID、チャレンジ応答を交換する。
4. UI側は接続相手のPIDを取得し、次を確認する。
   - 同じユーザーSIDであること
   - 同じログオンセッションであること
   - 実行ファイルの絶対パスがRELYRのインストールディレクトリ配下であること
   - 起動直後の親プロセスが想定したRELYR起動経路であること
5. ヘルパー側もUIプロセスのPID、SID、セッションID、実行ファイルパスを検証する。
6. チャレンジ応答が一致しない、PIDが変わる、検証に失敗する場合はコマンドを1つも処理せず切断する。

### 5.1 ブートストラップハンドルの引き渡し

- UIプロセスが匿名パイプを作り、秘密をバイト列として一度だけ書き込む。
- 子プロセスへ渡す匿名パイプの読取ハンドルだけを継承可能にする。
- `STARTUPINFOEX` の `PROC_THREAD_ATTRIBUTE_HANDLE_LIST` に、その読取ハンドルだけを明示して渡す。
- `bInheritHandles=true` を使う場合も、ハンドルリストにないハンドルは継承させない。プロセス内の全ハンドルを無条件に継承する実装は禁止する。
- 他のハンドルについては `SetHandleInformation(..., HANDLE_FLAG_INHERIT, 0)` を設定する。
- 秘密の値やハンドル値をコマンドライン・環境変数・設定ファイルに保存しない。コマンドラインで渡せるのは一時ハンドルを識別するための非秘密情報だけとする。
- 現在の昇格方式が `STARTUPINFOEX` の明示的ハンドルリストを昇格先まで渡せない場合、単純な `bInheritHandles=true` への切り替えは禁止する。既存の昇格ブローカーで明示的ハンドル引き渡しが可能かを先に確認し、できない場合は同一SID ACL・一回限りのランダム秘密・接続期限を持つブートストラップ専用パイプを代替案として設計レビューに戻す。
- 現行のタスクスケジューラ昇格ではハンドル継承が保証されないため、実装上の代替は一回限りのブートストラップ専用Named Pipeとする。ブートストラップPipeも同一ユーザーSIDの明示ACL、起動ごとのランダム名、10秒の接続期限、接続元のPID・SID・セッション・実行パス検証を持ち、秘密を一度送信したら直ちに切断・破棄する。通常IPCのコマンド受付には使用しない。

親子関係だけでは十分な認証にならないため、PID・SID・セッション・パス・一時秘密を組み合わせる。

### 5.2 PID検証のTOCTOU対策

- `GetNamedPipeClientProcessId` または `GetNamedPipeServerProcessId` でPIDを取得した直後に、`PROCESS_QUERY_LIMITED_INFORMATION | SYNCHRONIZE` 権限でプロセスハンドルを開く。
- 以後の実行パス、SID、セッションID、プロセス開始時刻の取得はPIDではなく、このプロセスハンドルに対して行う。
- `GetProcessTimes` で取得した開始時刻を接続時の識別子に含める。PIDだけの再検索は禁止する。
- ハンドシェイク完了までプロセスハンドルを保持し、`GetExitCodeProcess` または待機可能ハンドルで相手の終了を監視する。
- 検証中にPIDが再利用されても、保持しているプロセスハンドルの対象は変わらないため、別プロセスへの差し替えを受け入れない。

## 6. 許可するコマンド

汎用的な文字列実行、シェル実行、任意DLLロード、任意ファイル実行、任意ウィンドウメッセージ送信は提供しない。

許可候補は構造化された固定コマンドだけとする。

- `Ping`：接続確認
- `Shutdown`：UI終了時のヘルパー終了
- `InjectKey`：許可されたキー名とDown/Upだけ
- `InjectShortcut`：許可されたキーの有限長シーケンスだけ
- `WindowAction`：Close、Minimize、Maximize、Moveなど、既存の列挙値だけ。対象は原則としてヘルパー自身が取得した現在のフォアグラウンドのトップレベルウィンドウだけとする。
- `VirtualDesktopAction`：左右移動など既存機能に対応する列挙値だけ

各コマンドには最大サイズ、連続回数、タイムアウトを設定する。不明なコマンド、余分なフィールド、長すぎる入力、任意パスを含む入力は拒否する。

### 6.1 WindowActionの対象制限

- UIから任意のHWNDを受け取り、そのHWNDを管理者権限で操作するAPIは提供しない。
- 通常の`WindowAction`は、ヘルパーが実行直前に取得した現在フォアグラウンドの可視トップレベルウィンドウに限定する。
- ユーザーが明示的に対象を選択する機能が必要な場合は、ヘルパーが選択時点で発行する短時間・一回限りの対象トークンを使う。UIから生のHWNDだけを渡す方式は禁止する。
- 対象ウィンドウが変わった、閉じた、別セッションになった、またはトークンが期限切れの場合は操作を拒否する。

## 7. メッセージ形式

- 長さ付きUTF-8フレームまたは固定ヘッダー＋長さ＋ペイロードを使用する。
- `ProtocolVersion`、`RequestId`、`Command`、`Payload`、`Nonce` を持つ。
- `Payload` は固定スキーマとしてデシリアライズし、任意の型名を解決しない。
- 1リクエストにタイムアウトを設け、応答がない場合はUIをブロックしない。
- RequestIdとNonceで古い応答や重複要求を破棄する。

## 8. 起動順序と依存関係

1. UIプロセスを先に起動する。
2. UIプロセスが一時認証情報と接続待ち状態を作る。
3. UIプロセスが既存の昇格方式でヘルパーを1回だけ起動する。
4. ヘルパーがパイプへ接続し、ハンドシェイクを完了する。
5. ハンドシェイク完了後にのみグローバルフックと管理者アクションを有効にする。
6. 接続できない場合、UIは表示・Deck編集・Explorerドロップを継続し、管理者アクションだけを「現在利用できません」として安全に無視する。

無限リトライは行わない。再接続は指数バックオフで回数上限を設け、UIスレッドを待機させない。

## 9. ライフサイクルと異常時の動作

- UIプロセスはハートビートを一定間隔で送る。
- ヘルパーはパイプ切断またはハートビートタイムアウトで入力フックを解除し、短時間で終了する。
- UI終了時は`Shutdown`を送り、応答を待たずにヘルパーを終了させる。
- ヘルパーが異常終了した場合、UIは機能を停止せず、限定回数だけ再起動を試みる。
- 再起動失敗時に管理者権限での任意操作へフォールバックしない。
- 孤児プロセス検出は起動時と終了時に行うが、無関係なプロセスを終了させない。

## 10. セキュリティテスト

実装前に少なくとも次をテストする。

- 別ユーザーSIDから接続できない。
- 同一ユーザーの認証秘密を持たないプロセスが接続しても拒否される。
- PID、SID、セッション、実行パスが不一致の場合に拒否される。
- 不明コマンド、任意パス、過大ペイロード、重複RequestIdが拒否される。
- UI強制終了後にヘルパーが残らない。
- ヘルパー異常終了時にUIが固まらず、無限再起動しない。
- Explorerから非管理者UIのDeckへ複数回ファイルをドロップできる。
- 管理者対象ウィンドウへの既存アクションがIPC導入前と同じ結果になる。

## 11. 実コードとテスト証跡の継続レビュー条件

IPC変更後は、設計書だけで完了とはしない。次をレビュー対象として提示する。

- `PipeSecurity`を構築している実コード。ACLに`Everyone`、`Authenticated Users`、意図しない既定ACEが含まれていないこと。
- `STARTUPINFOEX` と `PROC_THREAD_ATTRIBUTE_HANDLE_LIST` の実コード。無条件の全ハンドル継承がないこと。
- 接続元プロセスハンドルを取得し、開始時刻・SID・セッション・実行パスを同じハンドルで検証している実コード。
- WindowActionがフォアグラウンドまたは短期対象トークンに限定されている実コード。
- 不正クライアント、ACL拒否、PID再利用想定、孤児化、ハートビート切断のテストログ。
- 各テストの入力、期待結果、実結果、終了コード。ログがない自己申告だけでは合格としない。

## 12. 実装時に確認した条件

次の条件は実装時の判断基準であり、今後の変更でも継続して満たす。

- 本設計書のPipeSecurity、接続元検証、許可コマンド、ライフサイクルをレビュー済み。
- 既存の昇格方式を再利用できるか確認済み。
- IPC導入で既存のマクロ、ジェスチャー、プロファイル切替、Deckクリックが壊れないテスト計画がある。
- テスト失敗時に旧方式へ戻せる切替方法がある。
