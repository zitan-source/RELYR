<Application x:Class="RELYR.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:RELYR">
    <Application.Resources>
        <SolidColorBrush x:Key="AppBackground" Color="#11141B"/>
        <SolidColorBrush x:Key="HeaderBackground" Color="#101720"/>
        <SolidColorBrush x:Key="FooterBackground" Color="#0B1118"/>
        <SolidColorBrush x:Key="PaneBackground" Color="#111923"/>
        <SolidColorBrush x:Key="SurfaceBackground" Color="#171D27"/>
        <SolidColorBrush x:Key="CardBackground" Color="#1D2330"/>
        <SolidColorBrush x:Key="ControlBackground" Color="#293142"/>
        <SolidColorBrush x:Key="ControlHoverBackground" Color="#354158"/>
        <SolidColorBrush x:Key="ControlPressedBackground" Color="#176B5D"/>
        <SolidColorBrush x:Key="InputBackground" Color="#171D27"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#465168"/>
        <SolidColorBrush x:Key="SubtleBorderBrush" Color="#33465A"/>
        <SolidColorBrush x:Key="PrimaryText" Color="#E8ECF4"/>
        <SolidColorBrush x:Key="SecondaryText" Color="#AAB4C8"/>
        <SolidColorBrush x:Key="MutedText" Color="#8F9CB2"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#72E0C1"/>
        <SolidColorBrush x:Key="AccentStrongBrush" Color="#1F8F7B"/>
        <SolidColorBrush x:Key="AccentButtonText" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="AccentSoftBrush" Color="#244B4A"/>
        <SolidColorBrush x:Key="WarningBrush" Color="#F6C66A"/>
        <SolidColorBrush x:Key="DangerBrush" Color="#E16A78"/>
        <SolidColorBrush x:Key="DangerBackground" Color="#7D2430"/>
        <SolidColorBrush x:Key="DangerHoverBackground" Color="#9B3040"/>
        <SolidColorBrush x:Key="DangerForeground" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="DangerHoverForeground" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="KeyBackground" Color="#1B2733"/>
        <SolidColorBrush x:Key="ReservedKeyBackground" Color="#343B44"/>
        <SolidColorBrush x:Key="LayerActiveBackground" Color="#146B5C"/>
        <SolidColorBrush x:Key="EditingKeyBackground" Color="#246BC5"/>
        <SolidColorBrush x:Key="EditingKeyBorderBrush" Color="#75B7FF"/>
        <SolidColorBrush x:Key="ActionKeyIconBrush" Color="#F09A3E"/>
        <SolidColorBrush x:Key="ActionDisabledIconBrush" Color="#AAB4C2"/>
        <SolidColorBrush x:Key="ActionProfileIconBrush" Color="#68A7FF"/>
        <SolidColorBrush x:Key="ActionShortcutIconBrush" Color="#52D5BE"/>
        <SolidColorBrush x:Key="ActionTextIconBrush" Color="#E4B936"/>
        <SolidColorBrush x:Key="ActionLaunchIconBrush" Color="#AA78DA"/>
        <SolidColorBrush x:Key="ActionMacroIconBrush" Color="#E15A65"/>
        <Style x:Key="AppButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="{DynamicResource PrimaryText}"/>
            <Setter Property="Background" Value="{DynamicResource ControlBackground}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="14,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="{DynamicResource ControlHoverBackground}"/>
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True"><Setter TargetName="ButtonBorder" Property="Opacity" Value="0.76"/></Trigger>
                            <Trigger Property="IsEnabled" Value="False"><Setter TargetName="ButtonBorder" Property="Opacity" Value="0.42"/></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="AccentAppButtonStyle" TargetType="Button" BasedOn="{StaticResource AppButtonStyle}">
            <Setter Property="Background" Value="{DynamicResource AccentStrongBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource AccentButtonText}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
        </Style>
        <Style TargetType="{x:Type ContextMenu}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource PrimaryText}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="HasDropShadow" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ContextMenu}">
                        <Border Background="{DynamicResource CardBackground}"
                                BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"
                                CornerRadius="7" Padding="6" SnapsToDevicePixels="True">
                            <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Cycle"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="{x:Type MenuItem}">
            <Setter Property="Foreground" Value="{DynamicResource PrimaryText}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Margin" Value="0,1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type MenuItem}">
                        <Border x:Name="MenuItemBorder" Background="{TemplateBinding Background}"
                                CornerRadius="5" Padding="{TemplateBinding Padding}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="18"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock x:Name="CheckMark" Text="✓" Foreground="{DynamicResource AccentBrush}"
                                           FontWeight="Bold" Visibility="Collapsed" VerticalAlignment="Center"/>
                                <ContentPresenter Grid.Column="1" ContentSource="Header"
                                                  RecognizesAccessKey="True" VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="MenuItemBorder" Property="Background" Value="{DynamicResource ControlHoverBackground}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="MenuItemBorder" Property="Opacity" Value="0.42"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="{x:Type Separator}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Separator}">
                        <Border Height="1" Background="{DynamicResource SubtleBorderBrush}" Margin="8,5"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Application.Resources>
</Application>
