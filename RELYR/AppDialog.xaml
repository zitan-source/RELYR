<Window x:Class="RELYR.AppDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="500" SizeToContent="Height" MaxHeight="720"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        ShowInTaskbar="False" Background="{DynamicResource SurfaceBackground}"
        Foreground="{DynamicResource PrimaryText}">
  <Window.Resources>
    <Style TargetType="Button" BasedOn="{StaticResource AppButtonStyle}">
      <Setter Property="MinWidth" Value="104"/>
      <Setter Property="Height" Value="40"/>
      <Setter Property="Margin" Value="6,0,0,0"/>
    </Style>
  </Window.Resources>
  <Border BorderBrush="{DynamicResource SubtleBorderBrush}" BorderThickness="1" CornerRadius="{StaticResource WindowCornerRadius}"
          Background="{DynamicResource SurfaceBackground}" Padding="24">
    <Grid>
      <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
      <Grid>
        <Grid.ColumnDefinitions><ColumnDefinition Width="44"/><ColumnDefinition/></Grid.ColumnDefinitions>
        <Border x:Name="IconBadge" Width="34" Height="34" CornerRadius="17" VerticalAlignment="Top"
                Background="{DynamicResource AccentSoftBrush}">
          <TextBlock x:Name="IconText" FontSize="21" FontWeight="Bold" HorizontalAlignment="Center"
                     VerticalAlignment="Center" Foreground="{DynamicResource AccentBrush}"/>
        </Border>
        <StackPanel Grid.Column="1">
          <TextBlock x:Name="HeadingText" FontSize="21" FontWeight="SemiBold" TextWrapping="Wrap"/>
          <ScrollViewer MaxHeight="420" VerticalScrollBarVisibility="Auto" Margin="0,12,0,0">
            <TextBlock x:Name="MessageText" FontSize="14" LineHeight="22" TextWrapping="Wrap"
                       Foreground="{DynamicResource SecondaryText}"/>
          </ScrollViewer>
        </StackPanel>
      </Grid>
      <StackPanel x:Name="ButtonPanel" Grid.Row="1" Orientation="Horizontal"
                  HorizontalAlignment="Right" Margin="0,24,0,0"/>
    </Grid>
  </Border>
</Window>
