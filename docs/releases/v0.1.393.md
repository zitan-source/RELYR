短押し・長押しActionの操作性と、ドラッグ＆ドロップ／Deckのアニメーションを改善しました。

- 右ペインのTAP／HOLD Actionを個別に削除・ドラッグ移動できます。
- ドロップ時は鮮明なアクセント色がキーの輪郭内で隅まで広がります。
- Deckのホバー待ち時間をなくし、指したボタンをすぐに判別できるようにしました。
- 高速でボタンを横切った際、古いアニメーションの完了処理が拡大状態を復活させる問題を修正しました。

警告ゼロのReleaseビルド、自己テスト、設定組み合わせ6,364件、UI・起動・終了テストを通過。配布ファイルはMicrosoft Defender検査で検出0件、SHA-256照合済みです。操作中のWindowsへの干渉を避けるため、実入力を注入するテストは実施していません。

新規導入は `RELYR-Setup-0.1.393.exe`、導入済みの場合は `RELYR-Update-0.1.393.exe` を使用してください。両方のSHA-256ファイルを添付しています。
